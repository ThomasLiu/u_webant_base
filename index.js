const user = require('./lib/model/user')
const global = require('./lib/model/global')
const login = require('./lib/model/login')
const menu = require('./lib/model/menu')

module.exports = {
  user,
  global,
  login,
  menu
}